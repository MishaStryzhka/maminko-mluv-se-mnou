# Future application routes

This directory is reserved for the future application migration. The current static landing page remains at repository root during the transition.

Planned route groups:
- public landing page
- checkout and payment result pages
- authenticated admin area
- server API routes

We will migrate into this structure incrementally so the existing Vercel deployment stays operational.
